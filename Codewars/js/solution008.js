//Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

//p we need a string to see t/f
//r we need to have a true or false statement
//e "duck " = true, 10 = false
// take in a parameter and run it and determine if string  or not

function boolToWord( bool ){
if(bool){
    return 'Yes'
}else{
    return 'No'
}
  }